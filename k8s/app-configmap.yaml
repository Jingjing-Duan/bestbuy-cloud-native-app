apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  MONGO_HOST: mongodb
  MONGO_PORT: "27017"
  RABBITMQ_HOST: rabbitmq
  RABBITMQ_PORT: "5672"
  EXCHANGE_NAME: order.created
  MAKELINE_QUEUE: makeline.queue
  INVENTORY_QUEUE: inventory.queue
  PRODUCT_DB_NAME: bestbuy_products